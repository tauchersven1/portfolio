name: Apply KO Certificate UI

on:
  push:
    branches:
      - agent/ko-certificates-fx
    paths:
      - .github/scripts/apply_ko_ui.py
      - .github/workflows/apply-ko-ui.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/ko-certificates-fx
          fetch-depth: 0

      - name: Apply UI patch
        run: python .github/scripts/apply_ko_ui.py

      - name: Commit generated change
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add name.abuchen.portfolio.ui/src/name/abuchen/portfolio/ui/wizards/security/SecurityMultiplierPage.java
          git commit -m "Add KO certificate master data fields"
          git push origin HEAD:agent/ko-certificates-fx
