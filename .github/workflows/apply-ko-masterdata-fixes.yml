name: Apply KO Master Data Fixes

on:
  push:
    branches:
      - agent/ko-certificates-fx
    paths:
      - .github/scripts/apply_fx_pair_consistency.py
      - .github/workflows/apply-ko-masterdata-fixes.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/ko-certificates-fx
          fetch-depth: 0

      - name: Apply KO master-data patch
        run: python .github/scripts/apply_fx_pair_consistency.py

      - name: Commit generated UI change
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add name.abuchen.portfolio.ui/src/name/abuchen/portfolio/ui/wizards/security/SecurityMultiplierPage.java
          git commit -m "Restore ISIN WKN derivative master-data lookup"
          git push origin HEAD:agent/ko-certificates-fx
